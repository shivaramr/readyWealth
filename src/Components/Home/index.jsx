import React, { memo } from "react";
import Footer from "../Footer";

const Home = memo(function Home(props) {
  return (
    <div>
      Home
      <Footer />
    </div>
  );
});

export default Home;
